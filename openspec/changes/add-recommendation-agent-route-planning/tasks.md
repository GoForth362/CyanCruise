## 1. 方向化存储与迁移

- [x] 1.1 将升学规划存储接口和内存实现改为按用户与具体方向读写、删除规划和每日任务。
- [x] 1.2 将 PostgreSQL 规划与每日任务主键升级为方向化复合键，并实现历史考研数据的幂等迁移。
- [x] 1.3 更新独立建表脚本、索引和资料归属校验，确保用户与方向双重隔离。

## 2. 保研智能规划后端

- [x] 2.1 扩展升学规划应用服务，使摘要、确保、生成、今日行动和任务更新始终使用当前保存方向。
- [x] 2.2 完成保研智能体预留配置、方向化输入、资料注入和结构校验，未配置或失败时不生成兜底数据。
- [x] 2.3 复用真实规划进度保护和每日任务持久化，确保保研更新不覆盖考研规划或任务。
- [x] 2.4 更新升学中心 WebAPI、统一路由和 DTO 文案，使保研资料、规划和今日行动可直接接入。

## 3. 保研页面

- [x] 3.1 在升学中心保研方向展示独立规划依据资料、真实规划状态和生成入口。
- [x] 3.2 让完整路径规划按保研方向展示阶段、本周计划和每日建议，无规划时只展示真实空状态。
- [x] 3.3 让今日行动按保研方向读取和更新任务，并在方向切换后恢复对应持久化状态。
- [x] 3.4 更新静态资源版本、路线/API 元数据和普通中文文案。

## 4. 验证与部署

- [x] 4.1 增加用户隔离、方向隔离、跨实例持久化和历史考研迁移测试。
- [x] 4.2 增加保研智能体未配置、有效输出、无效输出、旧规划保护和资料隔离测试。
- [x] 4.3 增加页面静态回归测试，确认不出现兜底路线、跨方向阶段或虚假今日行动。
- [x] 4.4 运行前端校验、OpenSpec 严格校验和 JDK 8 全量构建，并部署运行时资源。

## 5. 验收问题修复

- [x] 5.1 将保研调用调整为与考研一致的任务流编码优先策略，移除强制仅调用智能体编号的限制。
- [x] 5.2 配置保研任务流编码，增加直连参数回归测试并完成严格校验、JDK 8 构建与运行时部署。
