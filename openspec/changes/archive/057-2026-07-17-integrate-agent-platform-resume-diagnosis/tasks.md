## 1. 平台调用契约与配置

- [x] 1.1 在 `base-common` 定义简历诊断任务流请求、响应和平台调用结果公共 DTO。
- [x] 1.2 在应用层定义 Agent 平台任务流客户端边界、配置项和不可用状态，禁止硬编码平台地址、凭据或任务流编码。
- [x] 1.3 定义并记录任务流 `question` 请求 JSON 与 `answer` 响应 JSON 契约，保持与现有简历诊断 DTO 字段兼容。

## 2. 服务端任务流适配

- [x] 2.1 实现服务端 Agent 平台任务流客户端，支持配置化地址、鉴权、任务流编码和超时。
- [x] 2.2 实现 `AgentPlatformResumeDiagnosisAnalyzer`，组装简历正文、目标岗位、岗位要求和画像上下文，并校验任务流响应。
- [x] 2.3 在 `ResumeDiagnosisApplicationService` 中使用平台分析器，平台不可用或响应无效时有限重试；仍失败则返回可重试提示且不保存诊断。

## 3. 页面契约与安全验证

- [x] 3.1 保持既有简历诊断 WebAPI 和页面 DTO 契约，确认结果可直接包含总分、四项评分和结构化修改建议。
- [x] 3.2 补充平台成功、超时、无效 JSON、分数不一致和配置缺失的单元或集成测试。
- [x] 3.3 验证 resumeId 所有权校验、PDF 正文提取和平台调用日志均不泄露简历正文或平台凭据。

## 4. 平台联调与验收

- [x] 4.1 使用官方 Agent SDK 配置测试环境简历诊断智能体，验证 `query` 输入与诊断 JSON 输出。
- [x] 4.2 从 CyanCruise 简历诊断页面完整验证平台结果渲染、持久化，以及失败提示且不保存诊断的行为。
- [x] 4.3 执行 Gradle 构建和 OpenSpec 严格校验，并记录验证结果。
