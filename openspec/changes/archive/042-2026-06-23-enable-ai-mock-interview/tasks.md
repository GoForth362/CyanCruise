## 1. 公共契约与辅助规则

- [x] 1.1 新增模拟面试开始结果和回答轮次 DTO，保持 JDK 1.8 且不依赖框架或 AI SDK。
- [x] 1.2 实现面试上下文、中文问题提示词、报告提示词、结构化报告解析和本地降级规则。
- [x] 1.3 增加 helper 聚焦测试，覆盖上下文裁剪、问题降级、报告解析、分值限制和基础复盘。

## 2. AI 与应用编排

- [x] 2.1 重构 `InterviewAiService` 以复用 `AiGateway`，返回有效问题或结构化报告，并在异常时降级。
- [x] 2.2 扩展 `InterviewApplicationService`，按所有权加载简历和画像并实现开始、回答、结束复盘编排。
- [x] 2.3 保证仅进行中会话可回答、至少一条用户回答才可评分、已有报告直接复用。
- [x] 2.4 增加应用服务聚焦测试，覆盖上下文复用、消息顺序、降级、状态校验、报告缓存和画像同步。

## 3. Cosmic WebAPI

- [x] 3.1 新增开始并生成开场问题的一体化 WebAPI。
- [x] 3.2 新增提交回答并生成下一题的一体化 WebAPI。
- [x] 3.3 新增结束并读取或生成复盘的一体化 WebAPI。
- [x] 3.4 增加 WebAPI 聚焦测试，并确认既有细粒度接口保持兼容。

## 4. Webapp 用户流程

- [x] 4.1 将模拟面试路由接入专用状态和接口，复用当前身份、目标岗位和简历列表。
- [x] 4.2 实现中文准备页、文本对话页、结束操作、结构化复盘和历史查看。
- [x] 4.3 增加加载、防重复提交、空状态、错误与 AI 降级提示，避免专业缩写和行业黑话。
- [x] 4.4 更新 webapp 路由契约、README 与校验脚本预期。

## 5. 迁移文档与验证

- [x] 5.1 更新 IPD 迁移映射，记录来源、目标模块、数据映射、暂不迁移项和验证方式。
- [x] 5.2 运行 `openspec validate enable-ai-mock-interview --strict` 和全量 OpenSpec strict 校验。
- [x] 5.3 运行 webapp 路由校验及相关 Gradle 聚焦测试。
- [x] 5.4 设置 JDK 8 并使用仓库内 `gradlew.bat` 运行 `clean build`。

## 6. 面试方式路由修正

- [x] 6.1 拆分面试中心、AI 模拟面试和全景仿真面试路由，并修正就业页、顶部菜单和面试中心卡片跳转。
- [x] 6.2 按 `TEXT` 与 `VOICE/PANORAMA` 分类展示两类历史，AI 模拟面试页面只展示文字面试记录。
- [x] 6.3 为全景仿真面试提供独立边界页面和既有记录，不复用 AI 文字面试表单。
- [x] 6.4 更新路由契约、缓存版本并完成 OpenSpec、JavaScript 与 webapp 路由校验。

## 7. 沉浸式全景面试

- [x] 7.1 生成并接入无人物、无文字、无品牌的沉浸式面试房间贴图。
- [x] 7.2 实现摄像头与麦克风授权、实时预览、拒绝权限提示和媒体资源释放。
- [x] 7.3 实现全景面试问题、答题计时、浏览器语音转文字、手工输入降级和下一题流程。
- [x] 7.4 以 `VOICE` 模式保存全景面试会话与复盘，保持和 AI 文字面试历史分离。
- [x] 7.5 更新页面文档、路由契约和缓存版本，并完成 OpenSpec、JavaScript 与路由校验。

## 8. 普通 AI 模拟面试逐题体验

- [x] 8.1 在公共契约和应用服务中定义并强制最多 7 条有效回答，第 7 条后不再生成下一题。
- [x] 8.2 将 AI 模拟面试页面重构为单题展示、题号进度、语音转文字和手工回答降级。
- [x] 8.3 第 7 题提交后自动结束并生成复盘，结果页展示总分、能力评价、优点和具体改进方向。
- [x] 8.4 增加七题上限与自动复盘测试，更新页面文档、缓存版本并完成全量验证。

## 9. 首页面试推荐入口

- [x] 9.1 将首页推荐工具中的单个模拟面试卡片替换为全景仿真面试和 AI 模拟面试两张卡片。
- [x] 9.2 分别绑定 `interview-panorama` 与 `interview` 路由，更新缓存版本并完成页面校验。

## 10. 全景面试摄像头兼容降级

- [x] 10.1 增加当前窗口、同源父窗口和旧版 `getUserMedia` 的兼容请求链。
- [x] 10.2 区分非安全连接、平台嵌入限制、权限拒绝和设备占用提示。
- [x] 10.3 增加无摄像头继续模式，使问题、回答、计时和复盘不因摄像头限制而阻塞。
- [x] 10.4 更新缓存版本、文档和校验规则并完成全量验证。

## 11. 全景面试按难度自动计时

- [x] 11.1 规定入门、常规、进阶每题分别为 3、5、8 分钟，并在题目出现后自动开始倒计时。
- [x] 11.2 将语音按钮与计时解耦，倒计时结束后自动提交已有回答或记录超时未回答。
- [x] 11.3 更新全景面试文案、页面契约、README 与静态资源缓存版本。
- [x] 11.4 完成 JavaScript、路由契约和 OpenSpec strict 校验。

## 12. 全景面试背景通透展示

- [x] 12.1 降低全景页面总遮罩及问题、面试官、摄像头和回答面板的不透明度，保持背景与文字同时可见。
- [x] 12.2 更新缓存版本并完成页面资源和 OpenSpec strict 校验。

## 13. 面试完成状态与结果查看

- [x] 13.1 修复结束复盘的前端状态同步，使当前会话和历史记录立即显示已完成与最终分数。
- [x] 13.2 已完成记录点击后直接加载并展示结构化复盘，进行中记录仍可恢复答题。
- [x] 13.3 服务端修复已有报告但状态仍进行中的遗留会话，并增加聚焦测试。
- [x] 13.4 更新缓存版本并完成 JavaScript、Gradle 聚焦测试和 OpenSpec strict 校验。

## 14. 删除 AI 面试记录

- [x] 14.1 将既有 `/cc001/interview/delete` 接入苍穹自定义路由和 webapp 契约。
- [x] 14.2 在 AI 面试历史中增加带二次确认的删除操作，并处理成功、失败和当前会话清理。
- [x] 14.3 更新页面文档、缓存版本和校验标记。
- [x] 14.4 完成 JavaScript、路由、Gradle 聚焦测试和 OpenSpec strict 校验。

## 15. 语音回答按钮交互样式

- [x] 15.1 覆盖语音回答按钮的悬停、点击和焦点样式，避免全局深色背景遮挡文字与图标。
- [x] 15.2 更新缓存版本、样式校验标记并完成 OpenSpec strict 和页面校验。

## 16. AI 面试记录独立页面与分页

- [x] 16.1 新增 JDK 8 公共分页 DTO 和固定每页 10 条的 AI 面试历史应用服务契约。
- [x] 16.2 新增 `/cc001/interview/page` WebAPI 与苍穹自定义路由，按用户和文字面试类型分页。
- [x] 16.3 新增 `interview-history` 独立页面、上一页/下一页、查看结果和删除操作。
- [x] 16.4 从 AI 面试页和面试中心移除直接记录列表，改为“查看面试记录”按钮。
- [x] 16.5 更新路由契约、README、缓存版本和测试，完成 JDK 8、JavaScript 与 OpenSpec strict 校验。

## 17. AI 面试记录分页布局

- [x] 17.1 移除记录卡片中的“第 N 页”标题，仅在底部分页统计中展示页码。
- [x] 17.2 将分页栏调整为与记录卡片同宽，翻页按钮右对齐，并更新缓存版本与校验。

## 18. AI 面试记录时间

- [x] 18.1 在记录卡片中展示已完成面试的结束时间，以及进行中面试的开始时间和未结束状态。
- [x] 18.2 兼容现有时间序列化格式，更新缓存版本并完成页面与 OpenSpec strict 校验。

## 19. 面试问答详情与记录页精简

- [x] 19.1 删除 AI 面试记录页内容区重复的标题和返回卡片。
- [x] 19.2 为每条记录增加“查看问答”，按题号组合并展示持久化的问题与回答。
- [x] 19.3 支持从结果页查看问答、从进行中问答返回继续，并更新缓存版本与校验。

## 20. AI 面试中心返回路径

- [x] 20.1 面试答题、问答详情和结果页面的“返回”操作回到 AI 面试中心准备页，并保留面试记录。
- [x] 20.2 更新静态资源缓存版本和路由校验标记，完成页面验证与部署。

## 21. 首页统一面试中心入口

- [x] 21.1 将首页的“全景仿真面试”和“AI 模拟面试”两张卡片合并为“面试中心”卡片并跳转 `interview-home`。
- [x] 21.2 更新首页入口规格、缓存版本和页面校验，并完成部署。

## 22. AI 面试开始前保留表单选择

- [x] 22.1 在 AI 模拟面试开始请求的加载重绘中保留目标岗位、简历和练习难度，避免“入门”跳回“常规”。
- [x] 22.2 加固首页“面试中心”入口与表单状态校验，更新缓存版本并完成部署。
- [x] 22.3 在准备页字段变化时同步表单状态，并为页面资源增加缓存刷新标记。

## 23. 面试中心方式卡片布局

- [x] 23.1 将两种面试方式卡片改为标题、介绍、进入按钮的纵向内容顺序，避免介绍文字被压成窄列。
- [x] 23.2 保持两张卡片等高、按钮底部对齐，更新缓存版本和样式校验并完成部署。

## 24. 面试中心双记录入口

- [x] 24.1 在面试方式卡片下方并列展示“AI 模拟面试记录”和“全景仿真面试记录”两个独立入口。
- [x] 24.2 移除面试中心直接铺开的全景记录列表，统一记录入口布局、缓存版本和页面校验并完成部署。

## 25. 麦克风按钮悬停反馈

- [x] 25.1 为语音回答圆形麦克风增加悬停、键盘焦点和按下时逐级加深的颜色反馈，外层保持透明。
- [x] 25.2 为语音识别中的麦克风提供独立加深色，更新缓存版本、样式校验并完成部署。

## 26. 精简 AI 面试页顶部提示

- [x] 26.1 从 AI 模拟面试准备与答题页移除重复的“面试记录”入口卡和“暂时没有完成操作”提示卡。
- [x] 26.2 保留面试中心记录入口及答题区内联错误提示，更新缓存版本、页面校验并完成部署。

## 27. 全景仿真面试独立记录页

- [x] 27.1 新增 `interview-panorama-history` 路由和页面契约，复用 `/cc001/interview/page` 按 `VOICE/PANORAMA` 类型每页读取 10 条。
- [x] 27.2 实现全景记录分页、时间与状态展示、查看结果、查看问答、继续面试和删除操作。
- [x] 27.3 从面试中心进入独立全景记录页，并从全景准备页移除直接铺开的历史列表。
- [x] 27.4 增加全景类型分页聚焦测试，更新路由文档、缓存版本和校验并完成部署。

## 28. 全景面试人物、计时与空间比例

- [x] 28.1 为全景面试加入无文字、无品牌的 AI 面试官人物形象，替换抽象光球并保留清晰的身份说明。
- [x] 28.2 将每题计时改为基于截止时刻计算，使用户离开全景页面再返回时仍按实际经过时间继续倒计时。
- [x] 28.3 放大全景面试官与摄像头区域，缩小回答区高度、计时文字间距和无效留白。
- [x] 28.4 更新静态资源缓存版本与页面校验，并完成 OpenSpec strict、JavaScript、路由和部署验证。

## 29. 全景回答区紧凑布局

- [x] 29.1 继续压缩全景面试外层最小高度、顶部间距、问题卡片和回答面板空白，避免回答区占用过多屏幕。
- [x] 29.2 将回答按钮靠近输入框下方，缩小倒计时圆盘和输入框高度，保留背景可见性和可读性。
- [x] 29.3 更新静态资源缓存版本、页面校验标记，并完成 OpenSpec、JavaScript、路由和部署验证。

## 30. 全景 AI 面试官朗读题目

- [x] 30.1 全景面试题目不再直接展示正文，改为由 AI 面试官通过浏览器语音朗读，并提供“播放题目”兜底操作。
- [x] 30.2 朗读时为 AI 面试官人物卡片增加说话动效和状态文案，开始回答、提交、离开页面时停止朗读。
- [x] 30.3 更新静态资源缓存版本、页面校验标记，并完成 OpenSpec、JavaScript、路由和部署验证。

## 31. 全景 AI 面试官轻量数字人尝试

- [x] 31.1 尝试将全景面试官从静态真人图片改为前端 SVG 数字人头像，以验证轻量口型动画可行性。
- [x] 31.2 验证结果显示 SVG 数字人不符合真实面试官观感，因此不得作为最终展示方案。
- [x] 31.3 更新静态资源缓存版本、页面校验标记，并完成 OpenSpec、JavaScript、路由和部署验证。

## 32. 移除照片口型贴片

- [x] 32.1 移除照片口型贴片渲染和相关校验标记，避免在静态图片上覆盖假嘴造成突兀观感。
- [x] 32.2 保留浏览器朗读与说话状态契约，后续可替换为真实数字人视频服务。
- [x] 32.3 更新缓存版本并完成 OpenSpec、JavaScript、路由和部署验证。

## 33. 回退真人面试官展示

- [x] 33.1 将全景面试官展示回退为原真人图片，移除 SVG 数字人渲染。
- [x] 33.2 朗读时保留图片轻微动效和状态提示，不再展示假嘴或不自然数字人。
- [x] 33.3 更新缓存版本、页面校验标记，并完成 OpenSpec、JavaScript、路由和部署验证。
## 34. 摄像头和麦克风可用性诊断

- [x] 34.1 在全景面试准备页展示摄像头和麦克风检查结果，区分非安全连接、外层嵌入限制、浏览器权限、系统权限、设备缺失和设备占用。
- [x] 34.2 增加更宽松的摄像头请求重试，避免部分设备不支持高清参数时直接失败。
- [x] 34.3 更新缓存版本、页面校验标记，并完成 OpenSpec、JavaScript、路由和部署验证。
## 35. 摄像头和麦克风提示精简

- [x] 35.1 非安全连接导致浏览器隐藏媒体接口时，不再误提示浏览器版本不支持。
- [x] 35.2 将顶部错误提示统一为摄像头和麦克风不可用，并更新缓存版本、页面校验和部署验证。
