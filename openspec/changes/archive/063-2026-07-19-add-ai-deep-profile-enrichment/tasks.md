## 1. 深度画像数据与 AI 契约

- [x] 1.1 扩展职业画像快照，新增明确标识来源和生成时间的 AI 深度画像区块。
- [x] 1.2 增加 Agent 平台画像补全配置、请求适配器和严格 JSON 解析校验。
- [x] 1.3 实现深度画像应用服务，汇总已完成测评与事实画像、调用 AI 并保存最新有效结果。
- [x] 1.4 保存测评题目与所选项文本快照，并为单份已完成测评增加独立 AI 解读结果。
- [x] 1.5 在事实画像中增加“自画像补充”字段，并通过现有职业画像快照持久化。
- [x] 1.6 在职业画像快照中保存带记录标识的深度画像历史，并限制为最近 20 条。
- [x] 1.7 兼容旧版仅保存最新画像的快照，在首次读取历史或再次生成时将现存最新画像补入历史。

## 2. 服务端接口与下游上下文

- [x] 2.1 暴露生成和查询最新深度画像的受控 WebAPI，并接入自定义路由。
- [x] 2.2 将最新深度画像以“测评 AI 分析”上下文注入简历诊断、就业规划和升学规划。
- [x] 2.3 为缺少测评、未配置智能体、调用失败和非法响应补充可重试错误处理。
- [x] 2.4 提供单项测评 AI 解读的受控生成接口，并将解读写回该测评结果。
- [x] 2.5 将“自画像补充”作为用户事实注入深度画像与简历诊断上下文，避免重复建议已补充内容。
- [x] 2.6 提供当前用户深度画像历史列表和指定版本详情接口。

## 3. 画像补全页面

- [x] 3.1 增加页面请求状态、生成入口和最新深度画像加载。
- [x] 3.2 展示摘要、标签、偏好、证据说明、资料不足项和生成时间，并保持用户事实画像不被混淆。
- [x] 3.3 为生成失败显示明确错误和重试操作，不展示规则兜底结果。
- [x] 3.4 在单项测评结果页展示 AI 解读、证据不足和重新生成入口。
- [x] 3.5 在自画像“经历与优势”下提供“自画像补充”的编辑、保存和查看能力。
- [x] 3.6 将画像补全页改为最新摘要，并增加独立的深度画像详情与历史记录页面。
- [x] 3.7 已完成题组再次进入时默认展示最近结果和答题记录，仅在用户明确操作后重新测评。

## 4. 验证与部署

- [x] 4.1 添加关键应用服务和快照合并的单元测试。
- [x] 4.2 执行 OpenSpec 严格校验和 Gradle 构建。
- [x] 4.3 部署运行时资源并记录配置画像补全智能体编码、重启和浏览器测试步骤。
- [x] 4.4 添加历史持久化、用户隔离和详情查询测试。
- [x] 4.5 验证旧版画像补入历史、连续生成追加历史以及 PostgreSQL 存储往返。
- [x] 4.6 修复 Agent 工具字符串入参与 JSON 对象不匹配的问题，并增加 SDK 调用契约回归测试。
- [x] 4.7 改为通过 SDK 直接调用已配置的画像补全任务流，拒绝并隐藏内部工具调用指令，增加旧污染结果与严格结构校验回归测试。
