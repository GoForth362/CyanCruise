## 1. OpenSpec

- [x] 1.1 运行 strict validate，确认评分和页面规格可实施。

## 2. 评分与建议契约

- [x] 2.1 新增 JDK 8 评分明细 DTO，并扩展诊断结果和 helper 解析兼容。
- [x] 2.2 重构默认规则诊断器，实现四项 100 分评分和缺失信号驱动建议。
- [x] 2.3 增加 focused tests，覆盖分项合计、量化缺失、岗位匹配不足和旧结果兼容。

## 3. 结果页面

- [x] 3.1 展示评分标准、分项得分和评分依据。
- [x] 3.2 将优先级、简历区域、状态和上下文来源映射为普通中文，并清理用户可见 `JD`。
- [x] 3.3 优化具体建议结构并更新静态资源版本号。
- [x] 3.4 让目标岗位随所选简历自动更新并改为只读展示。
- [x] 3.5 增强岗位方向能力词匹配，并验证不同目标岗位影响评分和建议。
- [x] 3.6 诊断成功后刷新简历列表，确保结果分数与简历记录一致。
- [x] 3.7 移除建议处理状态按钮和无实际更新能力的简历更新入口。

## 4. 验证

- [x] 4.1 运行 helper/analyzer/application focused tests。
- [x] 4.2 运行前端语法、route 和 OpenSpec 校验。
- [x] 4.3 使用 JDK 8 `gradlew.bat` 运行相关构建。
