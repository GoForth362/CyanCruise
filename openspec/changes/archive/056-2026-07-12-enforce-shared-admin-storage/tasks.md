## 1. 共享运行配置

- [x] 1.1 为 `DebugApplication` 增加显式共享服务模式、Web 监听/公开地址和 PostgreSQL 配置注入，并避免将真实凭据写入受版本控制文件。
- [x] 1.2 在共享服务模式启动时校验 PostgreSQL 配置和连接可用性，输出不含敏感信息的明确失败原因。
- [x] 1.3 补充启动配置单元测试，覆盖局域网公开地址、完整配置和配置缺失场景。
- [x] 1.4 为标准星瀚服务的 `start-cosmic.bat` 补充共享运行参数，使 `8080` 与 `8881` 使用同一 PostgreSQL 状态后端；待重启 `8080` 后验证运行参数。

## 2. 共享持久化与身份边界

- [x] 2.1 调整存储工厂：共享服务模式下已迁移业务与管理员治理存储必须使用 PostgreSQL，禁止回退到进程内存。
- [x] 2.2 保持显式本地开发内存模式可用于测试，并使其不能被共享服务模式误用。
- [x] 2.3 为跨实例管理员治理读写、普通用户数据隔离、管理员保留用户功能和非管理员拒绝管理接口补充服务端测试。
- [x] 2.4 将订阅额度从进程内存迁移到 PostgreSQL，按用户和模板持久化并补充存储工厂回归测试。
- [x] 2.5 生产模式初始化始终调用服务端当前身份接口，账号变化或身份失败时不得继续使用浏览器缓存身份。
- [x] 2.6 将首页草稿和相关浏览器业务状态按服务端确认的 `userId` 分区，并迁移旧版未分区缓存。
- [x] 2.7 调整身份桥接优先级，使可信 Cosmic `RequestContext` 覆盖请求体携带的旧平台身份，并补充回归测试。

## 3. 管理后台故障可见性

- [x] 3.1 调整管理前端服务模块，使用户列表等读取请求的失败状态不再降级为空列表。
- [x] 3.2 在管理后台页面呈现中文可恢复错误状态和重试操作，并仅在成功空响应时显示“暂无用户数据”。
- [x] 3.3 补充前端静态/行为验证，覆盖成功空列表与请求失败两种状态。
- [x] 3.4 修复管理后台依赖的今日建议响应 DTO 序列化契约，避免 Hessian/Dubbo 响应递归失败。
- [x] 3.5 将无管理权限页面精简为居中的管理后台标题和开放范围说明，不展示技术性警告内容。
- [x] 3.6 修复用户管理搜索框事件，支持按名称、用户 ID、学校和专业查询并清空恢复。
- [x] 3.7 用户管理列表基于金蝶平台权限服务标注“管理员”或“普通用户”，并补充服务端和前端回归测试。

## 4. 验证与交付

- [x] 4.1 执行既有 PostgreSQL DDL 或核验表结构，确认共享数据库账号对已迁移业务表和 `cc_admin_*` 表具备读写权限。
- [x] 4.2 在两台电脑通过 `http://10.0.0.8:8881` 验证：普通用户仅见本人数据、两位管理员见同一治理数据、任一管理员写入对另一位可见。
- [x] 4.3 执行 OpenSpec 严格校验、相关模块测试和 JDK 8 `gradlew.bat clean build`，记录未覆盖的人工验证项。

## 人工验证记录

- 待完成 4.2：需在主机重启 `DebugApplication` 后，由两台电脑通过 `http://10.0.0.8:8881` 使用普通用户和两位管理员账号完成真实登录验证。
