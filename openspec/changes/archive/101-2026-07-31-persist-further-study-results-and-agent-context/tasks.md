## 1. 持久化存储

- [x] 1.1 实现 PostgreSQL 13 个升学功能输入表及当前用户隔离读写。
- [ ] 1.2 为 PostgreSQL 存储补充重启后读取及隔离测试。

## 2. 智能分析记录与上下文

- [x] 2.1 在三个升学 ApplicationService 的 13 项成功分析后保存输入和结果记录，并在择校择专业页面恢复当前用户的最新建议。
- [x] 2.2 在调用前先保存并从当前功能专属表回读输入，且不改变 WebAPI 输出 DTO。
- [x] 2.3 更新任务流系统提示词，约定唯一的 `mode`、`taskType`、`currentDate`、`payload` 输入结构和直接 JSON 输出。
- [x] 2.4 将升学分析调用链收敛为直接调用已发布任务流，移除智能体优先及回退分支。
- [x] 2.5 为保研导师联系增加本科就读院校字段，并确保任务流严格区分本科院校与目标院校。
- [x] 2.6 在其余 10 个非错题解析功能页重新进入时，按当前用户、方向和任务类型恢复并展示最新生成结果。

## 3. 验证

- [x] 3.1 执行后端模块编译校验。
- [x] 3.2 执行 OpenSpec 严格校验。
