## 1. 后端权限边界

- [x] 1.1 梳理 `AdminConsoleGovernanceWebApi` 中所有管理端接口，区分公共贡献接口和管理员接口。
- [x] 1.2 将所有管理员读写接口统一改为 `IdentityAwareCyanCruiseWebApiBoundary.requireAdmin()` 后再调用应用服务。
- [x] 1.3 为无权限、身份缺失、身份不匹配的管理端读接口提供稳定的失败返回或空状态，避免继续执行业务写入。

## 2. 前端管理端服务

- [x] 2.1 新增 `assets/services/admin-service.js`，封装 whoami、dashboard、users、questions、content、broadcast、audit log 等管理端请求。
- [x] 2.2 将管理端请求统一使用当前身份中的 `adminId` 或 `userId`，并兼容后端不可用状态。

## 3. 前端管理端页面

- [x] 3.1 扩展 `admin-console` 页面渲染为可用的管理工作台，包含总览、用户管理、内容管理、题库审核、通知公告、操作记录视图。
- [x] 3.2 补齐管理端按钮、空状态、错误提示和身份提示的中文文案，避免乱码和未解释缩写。
- [x] 3.3 普通用户或缺少身份时展示无权限状态，并阻止管理端数据加载。

## 4. 验证

- [x] 4.1 运行 `openspec validate extend-admin-console-mvp --strict`。
- [x] 4.2 运行前端路由校验或静态语法检查，确认新增服务被页面正确加载。
- [x] 4.3 使用 JDK 8 和仓库 `gradlew.bat` 运行聚焦测试或构建校验。

## 5. Cosmic 管理员体系对接

- [x] 5.1 新增 Cosmic 管理员权限解析扩展点，生产环境通过反射调用平台权限服务，失败时不放权。
- [x] 5.2 将平台管理员判断结果并入生产身份解析，补充 `ADMIN` 等价角色。
- [x] 5.3 增加单元测试覆盖平台管理员补权、普通用户不补权、平台权限服务不可用时 fail closed。
- [x] 5.4 运行 OpenSpec、聚焦测试和 JDK 8 构建校验。

## 6. 管理端视觉与布局优化

- [x] 6.1 将管理端从普通页面卡片改为独立后台工作台布局，包含深色侧边导航、顶部账号区和宽松内容区。
- [x] 6.2 优化管理端字重、间距、表格、状态徽标和操作按钮样式，减少拥挤感。
- [x] 6.3 运行前端语法检查和 OpenSpec 校验。
