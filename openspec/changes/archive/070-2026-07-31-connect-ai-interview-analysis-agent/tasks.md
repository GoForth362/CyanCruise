## 1. 面试智能体后端边界

- [x] 1.1 定义 JDK 8 兼容的面试分析接口、独立 `cc001.agent.platform.interview` 配置和默认客户端选择
- [x] 1.2 实现面试智能体任务流请求，提交岗位、难度、方式和已鉴权的完整文字问答
- [x] 1.3 实现结构化报告解析与严格校验，覆盖总分、有效回答数、五项得分与依据、优点、改进和总结

## 2. 应用流程与虚假分析清理

- [x] 2.1 将 `InterviewApplicationService` 的报告生成切换到面试智能体分析接口，成功后才保存报告、最终分数并同步画像
- [x] 2.2 删除 `fallbackReport`、默认 60 分字段补齐和固定优缺点；无回答或 AI 不可用时返回清晰错误且保留问答供重试
- [x] 2.3 保持普通面试和全景面试共用报告端点、存储与幂等读取，并兼容已有历史报告
- [x] 2.4 关闭允许浏览器直接提交最终分数或整份报告的旧 WebAPI，只允许服务端真实分析链路写入

## 3. 页面真实结果展示

- [x] 3.1 为普通 AI 模拟面试展示五项真实评分依据、做得好的地方和改进方向，并在生成失败时提供重试而不显示默认结果
- [x] 3.2 为全景仿真面试复用完整报告展示，说明只分析文字问答且不分析视频动作或原始音频
- [x] 3.3 更新静态资源版本、路由契约与部署说明，删除所有面试固定分析文案

## 4. 验证

- [x] 4.1 增加面试智能体配置、请求映射、有效/无效报告解析以及不可用不落库的后端测试
- [x] 4.2 增加普通与全景结果页契约校验，确认不存在按回答数计算的分数和固定评价
- [x] 4.3 执行 `openspec validate connect-ai-interview-analysis-agent --strict`、前端路由校验和 JDK 8 `gradlew.bat clean build`
- [x] 4.4 让本地调试启动器转发面试智能体运行时配置，并在忽略版本控制的本地配置中启用已发布智能体
- [x] 4.5 补齐统一 KAPI 对“无回答退出面试”安全端点的路由分发，并验证浏览器提交的分数或报告不会被采用
- [x] 4.6 根据真实联调中智能体偶发只返回 `Thought/Action/Action_input` 而未执行任务流的问题，将面试分析运行配置切换为直接调用已发布任务流，保持严格报告校验
