## 1. 留学智能体直连

- [x] 1.1 将留学规划运行配置收敛为 `enabled + agentNumber`，忽略留学 `taskFlowCode`。
- [x] 1.2 确保留学生成请求不携带任务流编码并继续完整传递画像、目标、进度和方向专属资料。
- [x] 1.3 将考研、保研和留学三个升学方向统一为 agent-only 装配，并清理实际 8080 启动脚本中的任务流编码。
- [x] 1.4 修正升学智能体查询的二次 JSON 编码，使 SDK 行为与平台预览中的原始 JSON 输入一致。

## 2. 验证与运行配置

- [x] 2.1 增加留学智能体直连、仅任务流配置不可用、单一 JSON 校验和三方向持久化隔离测试。
- [x] 2.2 更新本地配置示例和部署说明，明确留学只需智能体编码且不提供兜底数据。
- [x] 2.3 运行 OpenSpec 严格校验、相关测试与 JDK 8 构建，并报告尚需注入的实际智能体编码。
