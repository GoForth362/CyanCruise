# 任务

## 1. 修正已建对象文档

- [x] 1.1 以实际已建对象为准记录“用户职业画像”基础资料。
- [x] 1.2 以实际已建对象为准记录“Agent执行记录”单据。
- [x] 1.3 移除未实际采用的 `v620_billno` 要求。
- [x] 1.4 移除当前阶段不需要的 Agent“执行步骤”分录要求。
- [x] 1.5 将字段标识统一修正为平台保存后的 `v620_xxx` 形式。
- [x] 1.6 将待建对象编码统一压缩到金蝶设计器 25 个字符限制以内。

## 2. 补齐职业发展业务对象规划

- [x] 2.1 记录“职业测评记录”字段、表名和逻辑映射。
- [x] 2.2 记录“简历档案”字段、表名和逻辑映射。
- [x] 2.3 记录“简历诊断”字段、表名和逻辑映射。
- [x] 2.4 记录“今日任务”字段、表名和逻辑映射。
- [x] 2.5 记录“职业计划”字段、表名和逻辑映射。
- [x] 2.6 记录“模拟面试”字段、表名和逻辑映射。
- [x] 2.7 将“AI助手会话”修正为任务型“Agent能力定义”，记录字段、表名和逻辑映射。

## 3. 补齐深造陪伴业务对象规划

- [x] 3.1 记录“深造目标”字段、表名和逻辑映射。
- [x] 3.2 记录“深造记录”字段、表名和逻辑映射。
- [x] 3.3 记录“深造材料”字段、表名和逻辑映射。
- [x] 3.4 记录“深造进展记录”字段、表名和逻辑映射。

## 4. 补齐 Agent 运行管理对象规划

- [x] 4.1 保留“Agent执行记录”作为已建对象。
- [x] 4.2 记录“Agent上下文引用”作为后续可建对象。

## 5. 验证

- [x] 5.1 运行 `openspec validate model-cyancruise-business-objects --strict`。
- [x] 5.2 人工核对金蝶设计器中已建对象与文档一致。

## 6. 代码适配补齐

- [x] 6.1 增加 `CyanCruiseBusinessModelMapping`，将现有 storage boundary 的逻辑对象/字段映射到金蝶 `v620_cc_*` 业务对象和 `v620_*` 字段标识。
- [x] 6.2 增加 `MappedCosmicDatamodelGateway`，在不改动现有前端和业务服务调用的前提下，支持底层网关使用平台对象编码。
- [x] 6.3 保留现有 `cc_cl_*` 逻辑常量，新增 Agent、深造陪伴等已建业务对象的逻辑常量，避免破坏原有功能。
- [x] 6.4 补充映射网关回归测试，验证用户职业画像仍能按原接口读写，同时底层记录使用 `v620_cc_user_profile` 和 `v620_userid` 等平台编码。
